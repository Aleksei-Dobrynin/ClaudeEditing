import { Dayjs } from "dayjs";

export type document_approver = {
  
  id: number;
  step_doc_id: number;
  department_id: number;
  position_id: number;
  is_required: boolean;
  approval_order: number;
};
